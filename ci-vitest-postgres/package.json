{
  "name": "ci-vitest-postgres",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=dot",
    "db:setup": "node ./scripts/setup-db.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}